/bin/
/web/node_modules/
/web/dist/
/web/*.tsbuildinfo
/web/vite.config.js
/web/vite.config.d.ts

# Vite writes its build output here; nothing in this directory is checked in.
# Run `make web` before `go build` so //go:embed has files to embed.
/internal/assets/dist/
